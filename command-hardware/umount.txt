INFO (root) :
  |__ melepas mount dari disk

COMMAND PENGGUNAAN (hardware) :
- # umount /path/directory
  # umount /dev/path/device
  |__ lepas mount device

- # umount -R /path/directory
  |__ lepas mount folder
