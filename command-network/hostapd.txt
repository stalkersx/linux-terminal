INFO (root) :
  |__ buat hotspot on linux

COMMAND INSTALL :
- # sudo apt install hostapd

COMMAND PENGGUNAAN (network) :
- # hostapd <[path/file_configuration]>
  |__ buat akses point/hostpot, lalu lanjutkan dnsmasq untuk memberi client ip random
  
CONTOH FILE CONFIGURATION :
interface=ap0
driver=nl80211
ssid=wifilah
hw_mode=g
channel=10
wpa=2
wpa_passphrase=12345678
wpa_key_mgmt=WPA-PSK
