INFO (no-root) :
  |__ pencari text, file, atau folder dalam sebuah perintah

COMMAND PENGGUNAAN (language) :
- $ <[command]> | grep <[text]>
  $ cat ~/file | grep hello
  |__ temukan text dalam sebuah perintah

- $ <[command]> | grep -n <[text]>
  |__ temukan text dan nomor line dalam sebuah perintah
